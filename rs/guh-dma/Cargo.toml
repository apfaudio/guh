[package]
name = "guh-dma"
version = "0.0.0"
edition = "2021"
categories = ["embedded", "hardware-support", "no-std"]
description = "Small lib for pinned DMA buffers on RISCV"

[lib]
test = true
bench = false
